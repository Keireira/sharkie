---
title: Roadmap
description: Planned improvements and known issues
---

> **Disclaimer:** This documentation was auto-generated by Claude (Anthropic AI) and may contain inaccuracies. Always verify against the actual source code.

# Roadmap

## Implemented

| Feature | Status |
|---------|--------|
| Rate limiting (60 req/min per IP) | Done |
| Security headers (HSTS, nosniff, DENY, referrer-policy) | Done |
| Request body size limit (1 MB) | Done |
| Request timeout (30s → 408) | Done |
| Response compression (gzip + brotli) | Done |
| Cache headers (per-route, CDN-aware) | Done |
| Request ID tracing (UUID, structured logs) | Done |
| Query execution timing in logs | Done |
| Timezone-aware date clamping | Done |
| DB-aware health check (`SELECT 1`) | Done |
| Date range validation (5 year max) | Done |
| Graceful shutdown (SIGTERM + Ctrl+C) | Done |
| CORS (configurable via env) | Done |
| Dependabot (security-only updates) | Done |
| CI pipeline (clippy + biome + audit) | Done |

---

## Priority 1 — Reliability

### Database Connection Resilience

- Add connection retry logic on startup (currently panics if DB is unavailable)
- Add pool health monitoring with periodic `SELECT 1`

### Backfill Error Recovery

- Currently backfill stops on first API error. Should skip failed dates and retry later.
- Add backfill progress tracking so it can resume after restart

### SQLX Offline Mode

- Run `cargo sqlx prepare` and commit `.sqlx/` directory for CI builds without a running database

---

## Priority 2 — API Improvements

### Pagination

Add `limit` and `offset` parameters to `/history` with clamped max (e.g., `limit` max 366 days per request):

```
GET /history?from=2024-01-01&to=2024-12-31&limit=100&offset=0
```

### API Versioning

Prepare for future breaking changes:

```
GET /v1/history?...
```

### Content-Security-Policy

Add CSP headers with nonce-based script policy.

---

## Priority 3 — Infrastructure

### Monitoring

- Add Prometheus metrics endpoint (`/metrics`) via `metrics` + `axum-prometheus`
- Track: request count, latency histograms, error rates, DB pool stats, backfill progress
- Alert on: 5xx spike, DB pool exhaustion, backfill failures, rate limit hits

### CI Pipeline Enhancement

- Add build caching for faster CI runs
- Add E2E smoke tests post-deploy (hit `/health` endpoint)

### Backup Automation

- Automate dump downloads in CI/scheduled workflow
- Add dump validation (check row count, date range coverage)
- Add dump rotation policy

### Database

- Add read replica support for heavy query patterns
- Consider TimescaleDB for time-series optimizations on `exchange_rates` table
- Add `EXPLAIN ANALYZE` for slow query detection

---

## Priority 4 — Developer Experience

### Testing

- **Backend**: Add integration tests with `sqlx::test` for route handlers
- **Backend**: Add unit tests for date parsing, rate rebasing logic
- **Frontend**: Add Vitest + React Testing Library for component tests
- **E2E**: Add Playwright tests for critical user flows

### Documentation

- Add OpenAPI/Swagger spec for the API (via `utoipa` crate)
- Add ADR (Architecture Decision Records) for key decisions
- Add CONTRIBUTING.md

### Local Dev

- Add `docker-compose.test.yml` for running tests with a fresh database
- Add seed data script for development (smaller than full dump)

---

## Known Issues

| Issue | Severity | Notes |
|-------|----------|-------|
| Backfill stops on first error | Medium | Should skip and retry |
| No API versioning | Low | Will matter when breaking changes are needed |
| No CSP headers | Low | Should add for defense-in-depth |
